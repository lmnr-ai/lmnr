[project]
name = "lmnr-claude-code-plugin"
version = "0.1.0"
description = "Laminar observability plugin for Claude Code"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
