# Project Guidelines

## Package Manager

Always use `pnpm` for package management, not `npm`.

```bash
# Install dependencies
pnpm install

# Add a package
pnpm add <package>

# Run scripts
pnpm run <script>

# Type check
pnpm tsc --noEmit
```
