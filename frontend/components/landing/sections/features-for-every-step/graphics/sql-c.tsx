// Placeholder — variant c of "sql". Replace with the real graphic.
const SqlC = () => (
  <div className="flex size-full items-center justify-center text-xs text-foreground-500">sql / c</div>
);

export default SqlC;
