// Placeholder — variant b of "sql". Replace with the real graphic.
const SqlB = () => (
  <div className="flex size-full items-center justify-center text-xs text-foreground-500">sql / b</div>
);

export default SqlB;
