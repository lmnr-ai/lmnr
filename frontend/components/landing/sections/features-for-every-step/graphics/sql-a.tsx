// Placeholder — variant a of "sql". Replace with the real graphic.
const SqlA = () => (
  <div className="flex size-full items-center justify-center text-xs text-foreground-500">sql / a</div>
);

export default SqlA;
