// Placeholder — variant b of "search". Replace with the real graphic.
const SearchB = () => (
  <div className="flex size-full items-center justify-center text-xs text-foreground-500">search / b</div>
);

export default SearchB;
