// Base path for this section's two shared-trace reads. Rewritten to the
// production instance in next.config.ts, because the traces the mock renders
// live in that project and a local database has neither. Keep the two in sync.
export const SHARED_TRACE_API = "/api/landing-traces";
