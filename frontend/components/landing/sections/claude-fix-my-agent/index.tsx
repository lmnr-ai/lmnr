import DataPipeScene from "./data-pipe-scene";
import SectionCopy from "./section-copy";

const ClaudeFixMyAgent = () => (
  <section className="flex w-full flex-col items-start gap-10">
    <SectionCopy />
    <DataPipeScene />
  </section>
);

export default ClaudeFixMyAgent;
