"use client";

// Filler. The real global Laminar Agent panel lives in lmnr-private; this stub exists only so the
// OSS project layout compiles against the same import surface. It renders nothing.

export default function LaminarAgent() {
  return null;
}
