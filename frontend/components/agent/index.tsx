"use client";

// The Laminar Agent panel is not part of the open-source distribution; this stub keeps the
// shared surfaces (project layout, headers) identical across distributions.
export default function LaminarAgent() {
  return null;
}
