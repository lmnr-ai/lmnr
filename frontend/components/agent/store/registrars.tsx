"use client";

// Stubs — agent context registration is a no-op in the open-source distribution.

export function RouteAgentContext() {
  return null;
}

export function TraceAgentContext(_props: { traceId: string }) {
  return null;
}
