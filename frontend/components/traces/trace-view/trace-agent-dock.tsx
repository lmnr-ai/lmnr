"use client";

// Filler. The real trace-view agent dock host lives in lmnr-private; this stub exists only so the
// OSS trace-view content compiles against the same import surface. It renders nothing.

export default function TraceAgentDock() {
  return null;
}
