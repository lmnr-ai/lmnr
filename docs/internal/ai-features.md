# AI features model setup and Vercel AI SDK

<!-- Detailed working notes for coding agents and developers. -->
<!-- Referenced from the index in the repo-root CLAUDE.md; read when working in this area. -->
<!-- Sibling files in docs/internal/ may be cross-referenced by section name. -->

## AI Features Model Setup

- All frontend AI features (SQL generation, render-template generation, span previews, session extraction regex, score directions) go through `await getLanguageModel(feature, projectId?)` in `frontend/lib/ai/feature-model.ts`. `feature` is an `LlmFeature` enum member (`lib/ai/features.ts`; never pass the id string literal), and `LLM_FEATURE_ENV_TIER` names the env tier (`small` / `medium` / `large`) used when no `llm_feature_routes` row applies; the env-provider builder itself is `envLanguageModel(tier)` in `lib/ai/model.ts` and is only called from `feature-model.ts` and its test. See "LLM feature routes" below. Never add new tier names without updating every provider's `DEFAULT_MODELS`.
- SQL "Ask AI" generation modes (`GenerationMode` in `frontend/lib/actions/sql/types.ts`: `query` / `eval-expression` / `trace-expression` / `dataset-expression`) are coupled across THREE places that must stay in sync: the `GenerationMode` type, `MODE_CONFIGS` in `frontend/lib/actions/sql/prompts.ts`, AND the `mode` zod enum in `frontend/lib/actions/sql/generate.ts`. The generate action's enum is the validation gate — a mode missing there throws `Invalid option: ... at mode` at request time even though the type and prompt config accept it. Each table's columns menu picks its mode (`traces-columns-menu` → `trace-expression`, eval → `eval-expression`, dataset → `dataset-expression` over the `dataset_datapoints` table).
- In OSS, these env vars are read by the frontend only (chat-with-trace / SQL-with-AI). Signals — which also reuses these vars — is not an OSS feature; it ships in `lmnr-private`. Don't document Signals as an OSS feature in the OSS README or docker-compose files.
- Env provider (the fallback under the feature routes): `LLM_PROVIDER` (=`openai` | `gemini` | `bedrock` | `azure_chat_completions` | `azure_responses` | `azure_anthropic`; the app-server additionally accepts `openai_responses`) is the single switch — there are no per-feature provider vars any more (`SIGNALS_PARSING_LLM_PROVIDER`, `INPUT_EXTRACTION_LLM_PROVIDER`, `SP_EXTRACTION_LLM_PROVIDER`, `AGENT_LLM_PROVIDER` were deleted in favor of `llm_feature_routes`). The app-server registers only that one provider; Bedrock no longer auto-registers on AWS credentials alone. `openai` and `gemini` use `LLM_API_KEY` (+ optional `LLM_BASE_URL`; `openai` is OpenAI-compatible and works with LiteLLM proxy / OpenRouter / vLLM). `bedrock` uses AWS credentials (`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_REGION`) instead of `LLM_API_KEY`. `LLM_MODEL_SMALL` / `LLM_MODEL_MEDIUM` / `LLM_MODEL_LARGE` are optional overrides; each provider has hard-coded defaults in `DEFAULT_MODELS`. **Those defaults are duplicated in two places that must be bumped together** — `DEFAULT_MODELS` here and `model_for_size` in `app-server/src/llm/mod.rs`; when they drift, the UI and the server call different models (on the `azure_*` providers, different *deployment names*, so one side 404s while the other works). Bumping an Anthropic-backed default also means checking `bedrock::requires_adaptive_thinking` — a model outside that list gets the legacy `budget_tokens` thinking shape and `temperature`, both 400s on Opus 4.7+ / Claude 5.x. Mirror the app-server's provider matching in `app-server/src/features/mod.rs` / `llm/mod.rs` when extending — a provider the app-server accepts but `getConfiguredLLMProvider` doesn't silently turns `Feature.SIGNALS` off and makes every UI AI feature throw.
- **Azure provider names are `azure_<api-shape>`, and the shape is the only thing that varies.** One Azure AI Foundry resource fronts three unrelated APIs on one host, so `azure` alone is ambiguous; the name says which wire format is being spoken, not which behaviour is wanted:

  | `LLM_PROVIDER` | Endpoint | Wire format | Auth header |
  |---|---|---|---|
  | `azure_chat_completions` | `<resource>.services.ai.azure.com/openai/v1/chat/completions` | OpenAI | `api-key` |
  | `azure_responses` | `<resource>.services.ai.azure.com/openai/v1/responses` | OpenAI Responses | `api-key` |
  | `azure_anthropic` | `<resource>.services.ai.azure.com/anthropic/v1/messages` | Anthropic Messages | `x-api-key` |

  Because it is one resource, the env is shared: `LLM_API_KEY` + `AZURE_RESOURCE_ID` (or `AZURE_BASE_URL` for private endpoints/gateways), resolved once in `app-server/src/llm/azure.rs::resource_root` and once in `azureEndpoint()` in `lib/ai/model.ts`; each client appends its own path. Rust const names are spelled identically to the var they hold. Keep that shape when adding a fourth API shape. There are no back-compat aliases — a stale `LLM_PROVIDER=azure_openai` doesn't error, it falls through `has_llm_provider()` to `No LLM-backed feature enabled` and every AI feature silently turns off.
- **The OpenAI-shaped routes and the Anthropic route are different endpoints, not aliases.** A Claude deployment has **no** OpenAI-compatible route — sending an OpenAI-protocol request there 404s, and adding `?api-version=` or rewriting to `/openai/...` is what produces the "resource not found" reports. App-server-side, `llm/azure_anthropic.rs` reuses `bedrock`'s `build_request_body`/`parse_response_body`/`BedrockStreamAccumulator` because Bedrock's InvokeModel body *is* the Anthropic Messages body (it is not the Converse API); the Azure-specific parts are the auth + `anthropic-version: 2023-06-01` headers, dropping the Bedrock-only `anthropic_version` body field, and SSE framing instead of the AWS event stream. Auth: **use `x-api-key`** — Anthropic's Azure docs list both `api-key` and `x-api-key`, but `api-key` 401s against a real deployment (verified against ours), so both services send `x-api-key` and neither needs a header shim. `Authorization: Bearer` is the separate Entra ID path. Note this is the exact opposite of the two OpenAI-shaped siblings, which authenticate with `api-key` — the same resource disagrees with itself on the header depending on the route.
- **The OpenAI-shaped Azure providers** reuse the OpenAI clients on both sides; only the endpoint and auth differ. Everything normalizes to `https://<resource>.services.ai.azure.com/openai/v1`, which serves both `/chat/completions` and `/responses`. Auth is the `api-key` header, NOT `Authorization: Bearer` (bearer is AAD-token-only). `AZURE_API_VERSION` is optional: the v1 route is GA and needs none, but some resources still reject requests without an explicit `?api-version=` (e.g. `preview`). Model ids are **deployment names** — the hard-coded defaults assume deployments named after the model, so `LLM_MODEL_*` is effectively required otherwise. Frontend gotcha: `createAzure` special-cases `*.openai.azure.com` hosts — it appends `/v1` and sets `?api-version=` only for those — and the Foundry host is **not** one of them, so `lib/ai/model.ts` hands it the full `/openai/v1` URL plus a `fetch` shim that adds `api-version`; the `/openai`-root branch survives only for an `AZURE_BASE_URL` pointed at a legacy Azure OpenAI resource. `createAzure`'s `resourceName` option is unusable for the same reason (it builds `<name>.openai.azure.com`). Also note `azure(id)` returns a **Responses** model — `azure_chat_completions` must call `azure.chat(id)`. `openai_responses` still resolves to `openai` on the frontend, since that split is backend-only.
- **A signal with a pinned profile ignores all of this.** With `LAMINAR_CLOUD` unset, a signal can pin a workspace LLM profile + model (see `docs/internal/signals.md` → "LLM Profiles"); that pin wins over the feature routes and the env provider alike.
- `isAiProviderConfigured()` (frontend) and `has_llm_backend()` (`app-server/src/features/mod.rs`) are true when EITHER a complete env provider is configured OR `LLM_SYSTEM_WORKSPACE_ID` holds a UUID — the latter means server-side features can run on feature routes with no env provider at all. Note the asymmetry with the runtime: a feature whose lookup finds no route and no env provider throws per call (`No LLM route or AI provider configured for this feature` / `No LLM configured for feature '<id>'`); the flag does not check that a `default` route exists.
- `Feature.SIGNALS` requires `SIGNALS_ENABLED=true` AND (`isAiProviderConfigured()` OR `Feature.SIGNAL_LLM_PROFILES`, i.e. `LAMINAR_CLOUD !== "true"`). `Feature.BATCH_SIGNALS` is currently disabled via the feature flag (`return false`) until the batch path is re-enabled; when re-enabling, gate on `LLM_PROVIDER === "gemini"` since the backend batch API is Gemini-specific.
- The Rust OpenAI client (`app-server/src/llm/openai/`, gated behind the `signals` feature — `allow(dead_code)` in OSS) is the path Signals hits. `provider_request_to_openai_body` (`conversions.rs`) must NOT forward `reasoning_effort` when **function tools are present**: gpt-5.x reasoning models 400 on `/v1/chat/completions` with "Function tools with reasoning_effort are not supported … use /v1/responses instead" — this fires on OpenAI direct AND on some OpenAI-compatible gateways (LiteLLM/OpenRouter) that forward to OpenAI, so endpoint-sniffing is unreliable. The guard is simply `if !has_tools` — forward `reasoning_effort` only when no tools are present, regardless of endpoint.
- The `signals` feature does NOT compile standalone in the OSS `lmnr` repo — it pulls in clustering worker variants (`BatchWorkerType::ClusteringBatching`, `WorkerType::Clustering`) that live in `lmnr-private`, so `cargo test --features signals` fails with E0599 in `main.rs`. Run `llm/openai` tests under default features (`cargo test --bin app-server llm::openai::conversions`); the test modules are not feature-gated.
- **Effort naming differs per provider for `ProviderThinkingLevel::XHigh`:** OpenAI's `reasoning_effort` tier above `high` is `"xhigh"`, but Bedrock's adaptive-thinking `output_config.effort` scale tops out at `"max"` (Anthropic naming; `"xhigh"` is a ValidationException, and `"max"` is Opus 4.6+-only). Gemini has no tier above HIGH — its `ThinkingLevel` accepts the provider enum's `X_HIGH` via a serde alias and collapses it to HIGH. Don't unify these strings across providers.

## LLM feature routes (`llm_feature_routes`, LAM-2248)

- **What it replaces.** Per-feature `*_LLM_PROVIDER` env vars and hardcoded `ModelSize`s. Every server-side LLM call now belongs to one *feature id* and asks the table which profile + model to run on. Features that used to differ only by size are separate ids (`input_extraction_direct` vs `input_extraction_regex_generation`, `checkpoints_classifier` vs `checkpoints_system_prompt`, …) so each can be routed on its own.
- **Table.** `id`, `workspace_id uuid NULL`, `feature_id text`, `llm_profile_id`, `model_name`, timestamps. `UNIQUE NULLS NOT DISTINCT (workspace_id, feature_id)` (Postgres ≥ 15) so there is exactly one global row per feature. FK `(llm_profile_id, model_name) → llm_profile_models ON DELETE RESTRICT` — a routed model cannot be dropped from its profile, and profile deletion 409s with the feature ids still routed to it (`CrudError::InUse`). FK `(workspace_id, llm_profile_id) → llm_profiles(workspace_id, id)` (backed by the redundant `llm_profiles_workspace_id_id_key` unique) keeps a workspace row on its own profile; it is `MATCH SIMPLE`, so it is skipped for global rows. Migration `0111_llm_feature_routes`: the `llm_profiles` unique constraint was moved to the top by hand because drizzle-kit emitted the FK that depends on it first.
- **Ids are one string namespace across two lists**: `LlmFeature` in `app-server/src/llm/features.rs` (`as_str` is the `feature_id` value; only the features the app-server runs) and the `LlmFeature` string enum in `frontend/lib/ai/features.ts` (only the ids the frontend runs; `LLM_FEATURE_ENV_TIER` maps each to its env tier). Adding a feature = add the variant + string + `env_size` arm in Rust, or the enum member + tier entry in the frontend. `default` is the table-level fallback feature. Nothing validates `feature_id` on write — a typo'd row is simply never matched.
- **Resolution order** (identical in `LlmProfileStore::resolve_feature` and `frontend/lib/ai/feature-model.ts`): (1) an explicit profile pin on the call (`LlmRoute::Profile`: signals with a pinned profile, playground) → (2) the project's workspace row for the feature → (3) the workspace's `default` row → (4) the global (`workspace_id IS NULL`) row for the feature → (5) the global `default` row → (6) the env provider at `LlmFeature::env_size()` / the tier in `FRONTEND_LLM_FEATURES`. A call with no project (frontend `evaluation_score_directions`, `LlmRoute::feature(f, None)`) skips (2)–(3). Workspace rows outrank global ones on purpose: a workspace override is the only way to move one tenant without touching everyone else.
- **Global rows and the system workspace.** `LLM_SYSTEM_WORKSPACE_ID` (`env::llm::SYSTEM_WORKSPACE_ID`, same name on the frontend) names the Laminar-internal workspace whose profiles hold Laminar's own keys; global rows point at that workspace's profiles by convention (the composite FK is `MATCH SIMPLE`, so it does not check null-workspace rows). Its code effect is only the feature flag: `has_llm_backend()` / `isAiProviderConfigured()` are true without an env provider when it is set. There are no nullable-workspace *profiles* — every profile stays workspace-owned and billable.
- **There is no app write path.** Rows are inserted/updated/deleted directly in Postgres (the app-server only has `db/llm_feature_routes.rs::get_scope_routes` and `feature_ids_using_profile`). The FKs are the only validation: `(llm_profile_id, model_name)` must be an `llm_profile_models` pair and a workspace row's profile must belong to that workspace. Profile deletion still 409s naming the routed feature ids. No UI, no CLI.
- **Caching** (`llm/profiles/store.rs`). Steps (2)–(5) run as two explicit scope lookups, workspace then global (`lookup_route` → `scope_route`); each scope reads its `{feature}` and `default` keys from the cache and, if either is missing, loads both rows with one query (`db/llm_feature_routes.rs::get_scope_routes`, `workspace_id IS NOT DISTINCT FROM $1 AND feature_id = ANY(...)`) and caches both. One shared-cache key per scope and feature: `llm_feature_route_v2:{workspace_id}:{feature}` and `llm_feature_route_v2:global:{feature}`. The prefix is `_v2` because the retired `llm_feature_route` prefix cached the *resolved* target under the caller's workspace key, where `Absent` meant "nothing in any scope" — a replica still on that layout reading a v2 `Absent` would skip the global rows, so the layouts must not share keys during a rollout (the old entries simply expire). The `global:*` keys are shared by every workspace that falls through to them and a scope's `default` key by every feature that falls through to it — so a new workspace's first call costs one query (its own scope), and a fully cold process two. **Misses are cached too** (`Absent`), since most lookups miss. Because writes bypass the app, nothing invalidates these entries — the 5-minute TTL (`LLM_FEATURE_ROUTE_CACHE_TTL_SECONDS`) is the propagation delay for a route change, and it is the same for every entry. A longer TTL on the `default` keys was tried and reverted: a scope re-queries as soon as *either* of its keys is missing and rewrites both, so the `{feature}` key's TTL alone sets the query rate and a long-lived `default` entry saved nothing. A long TTL on every key would cut the (already negligible: one indexed query per scope and feature per 5 minutes fleet-wide) Postgres load, but every route edit would then need a `redis-cli del` of the right key, and deployments on the in-memory cache (no Redis) would need a restart — not worth it. To see what prod resolved: `redis-cli --scan --pattern 'llm_feature_route_v2:*'` and `GET` the keys; a value of `"Absent"` means that scope had no row for the feature, `{"Route":{…}}` is the row itself (never the inherited one — inheritance happens at read time, so a workspace on the global default shows `Absent` under its own keys and the row under `global:default`). Profile rows keep their 7-day TTL (the profile service removes the key on write). Built provider clients are reused in-process while the profile's `updated_at` is unchanged. Project, route and profile lookup DB/Redis failures are `ProviderError::RequestError` (retryable) on purpose — a blip must not silently reroute traffic to the env provider, nor be reported as a permanent misconfiguration; only "row missing / model not on profile" is `ConfigError`. The frontend does not cache: one indexed query per call.
- **Where the project id comes from** (needed for the workspace layers): agent `ctx.project_id`, checkpoints `CheckpointRoot::origin_project_id()`, clustering `ClusterNamingRequest.project_id`, input extraction `SpanScope.source_project_id`, static-prompt extraction `ExtractionTracing.source_project_id` (Option), signals `msg.project_id`, reports `project.id`. `signal-test` / `signal-eval` and `static-sp/extract` lost their `provider` / `modelSize` request fields — the route decides.

## Vercel AI SDK (v7)

- The frontend is on `ai` v7 (`@ai-sdk/*` providers at 4.x, `@ai-sdk/react` 4.x, `@ai-sdk/amazon-bedrock` 5.x). Playground chat and all server-side AI features go through it.
- **`LanguageModelUsage` is nested in v7** (was flat in v5). Shape: `{ inputTokens, inputTokenDetails: { noCacheTokens, cacheReadTokens, cacheWriteTokens }, outputTokens, outputTokenDetails: { textTokens, reasoningTokens }, totalTokens }`. Cached-input and reasoning tokens live UNDER `inputTokenDetails`/`outputTokenDetails` — there is no top-level `cachedInputTokens`/`reasoningTokens`. Any literal usage object (store initial state, fallbacks) must use this nested shape or it won't type-check.
- **`GenerateTextResult` takes THREE generics in v7**: `GenerateTextResult<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context, OUTPUT extends Output>`. We use `GenerateTextResult<ToolSet, Record<string, never>, never>`. `Context` is NOT re-exported from `ai` (it lives in `@ai-sdk/provider-utils`); use `Record<string, never>` for the runtime-context slot rather than importing it.
- `GenerateTextResult` has both `usage` and a deprecated `totalUsage` (both `LanguageModelUsage`). Prefer `result.usage` for display; `totalUsage` is only kept where the struct requires it.
- `result.reasoning` is `Array<ReasoningOutput | ReasoningFileOutput>` — `ReasoningFileOutput` has no `.text`, so guard with `("text" in r ? r.text : "")` before reading it.
- **Span attributes use v7 usage keys**: `result.usage.inputTokens` / `.outputTokens`, NOT v4's `.promptTokens` / `.completionTokens` (which are now `undefined`). `SpanData.result` is typed `any` in `lib/actions/chat/utils.ts`, so wrong keys silently emit `undefined` instead of failing to compile — verify against the nested shape.
- Telemetry is registered globally once via `registerTelemetry(new LaminarAiSdkTelemetry())` (`registerTelemetry` from `ai`, `LaminarAiSdkTelemetry` from `@lmnr-ai/lmnr`), called in `instrumentation.ts` right after `Laminar.initialize()`. With global telemetry registered, per-call `experimental_telemetry` / `tracer` options are NOT needed — `generateText`/`generateObject` calls are traced automatically. Do not add `experimental_telemetry: { isEnabled: true }` or a per-call `tracer: getTracer()` to call sites.
- `generateText` / `generateObject` use `maxOutputTokens` (not `maxTokens`) for the output cap; our form field is still named `maxTokens` and is mapped at the call site.
- The playground `ToolResultPart["output"]` shape is defined locally in `lib/playground/types.ts` (carries a `media` content variant), NOT imported from `ai` — v7's `ai` `ToolResultPart` is now generic over `ToolSet` and its `output` no longer matches the form's shape, so import the local type in `message-parts.tsx`.
- **Span-view rendering of native AI-SDK v7 prompts** (`lib/spans/types/ai-sdk.ts`, `parseAiSdkMessages`). The detector handles BOTH vintages the SDK has stored: (a) verbatim LanguageModel-level payloads from `gen_ai.input.messages` / `gen_ai.output.messages` — dash-typed parts (`tool-call`, `tool-result`, `reasoning`), `file` parts with top-level `data|url` + `mediaType`, `providerOptions`/`providerMetadata` intact; (b) the legacy server-reshaped `ai.prompt.messages` shape — snake_case `tool_call` parts, JSON-stringified assistant content, and tool results stored as `{type:"text"}` parts inside `role:"tool"` messages (historical ClickHouse rows keep this shape forever). It runs in `processMessages` BEFORE the OpenAI detectors (the loose OpenAI schemas would otherwise claim verbatim payloads and drop `reasoning` parts), gated by `hasAiSdkDiscriminator`: a message-level `providerOptions`/`providerMetadata`, a distinctive part type, or a LanguageModel `file` part (top-level `data|url` + string `mediaType` — OpenAI nests its file payload under a `file` key, so it never matches). Plain `[{role, content:"str"}]` chats are NOT claimed and fall through to OpenAI/generic. `parseAiSdkMessages` routes to the `type:"generic"` renderer; `matchAiSdkMessages` also drives the playground branch in `convertSpanToPlayground` (`lib/spans/utils.ts`). Detection ordering is pinned by `tests/test-process-messages-detection.test.ts`, including the `{role, parts}` (GenAI) vs `{role, content}` (AI SDK) mutual non-claim.
- **Do not add v7 part types to `convertToMessages`'s `processContentPart`** to fix this — that helper is shared with the playground; the span-view fix belongs in the dedicated `ai-sdk.ts` detector, which routes to the `type: "generic"` renderer (`components/traces/span-view/generic-parts.tsx`) that already handles `reasoning`/`tool-call`/`tool-result`.
- **The generic renderer's `default` switch case renders a labeled JSON block, not `undefined`.** v7 broadened `ModelMessage` with parts that have no dedicated UI (`custom`, `reasoning-file`, `tool-approval-request`, `tool-approval-response`); `parseAiSdkMessages` passes them through verbatim and `generic-parts.tsx` surfaces them as JSON so nothing is silently dropped. Add a dedicated `case` only when a part type warrants bespoke UI.
